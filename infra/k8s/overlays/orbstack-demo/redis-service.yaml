apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: clubcrm-data
spec:
  clusterIP: None
  selector:
    app.kubernetes.io/name: redis
  ports:
    - name: redis
      port: 6379
      targetPort: 6379
