apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: redis-data
  namespace: clubcrm-data
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: clubcrm-longhorn
  resources:
    requests:
      storage: 2Gi
